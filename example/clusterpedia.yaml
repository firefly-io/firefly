apiVersion: install.firefly.io/v1alpha1
kind: Clusterpedia
metadata:
  name: demo
  namespace: firefly-system
spec:
  controlplaneProvider:
    karmada:
      name: demo 
    syncResources:
    - group: ""
      resources:
      - "pods"
      - "services"
    - group: "apps"
      resources:
      - "*"